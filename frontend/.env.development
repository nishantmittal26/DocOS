# DocOS Frontend - Local Development Environment (Loaded by default when running 'npm run dev')
# Routes all API calls through Vite's local dev proxy to the .NET backend (http://127.0.0.1:5107)
VITE_API_URL=/api
